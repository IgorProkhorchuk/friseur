services:
  postgres:
    image: postgres:16
    container_name: friseur_test_postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata_test:/var/lib/postgresql/data
    networks:
      - friseur_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: ${APP_BUILD_CONTEXT:-../../..}
      dockerfile: ${APP_DOCKERFILE:-infra/docker/app/Dockerfile}
      target: ${APP_BUILD_TARGET:-runtime-from-source}
    image: friseur:test
    container_name: friseur_test_app
    environment:
      SPRING_DATASOURCE_URL: ${SPRING_DATASOURCE_URL}
      SPRING_DATASOURCE_USERNAME: ${SPRING_DATASOURCE_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${SPRING_DATASOURCE_PASSWORD}
      SERVER_PORT: ${SERVER_PORT:-8080}
      SESSION_SECURE_COOKIE: ${SESSION_SECURE_COOKIE:-false}
      REMEMBER_ME_KEY: ${REMEMBER_ME_KEY:-friseur-local-dev-remember-me-key-change-me}
      REMEMBER_ME_SECURE_COOKIE: ${REMEMBER_ME_SECURE_COOKIE:-false}
      REMEMBER_ME_TOKEN_VALIDITY_SECONDS: ${REMEMBER_ME_TOKEN_VALIDITY_SECONDS:-2592000}
    ports:
      - "8081:8080"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - friseur_net
    volumes:
      - app_logs_test:/var/log/friseur

volumes:
  pgdata_test:
  app_logs_test:

networks:
  friseur_net:
